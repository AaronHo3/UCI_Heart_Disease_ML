# Slim dependency set for tests + CI (excludes the heavy shap/numba stack that
# only the interpretability analysis needs). Pinned to the versions the project
# was developed against.
numpy==2.4.2
pandas==3.0.1
scikit-learn==1.8.0
scipy==1.17.1
matplotlib==3.10.8
pytest==9.1.0
pytest-cov==7.1.0
ruff==0.15.17
