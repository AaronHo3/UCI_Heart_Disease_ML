# Direct runtime dependencies, pinned to the versions the committed results were
# generated against (see reports/run_manifest.json). Transitive dependencies are
# resolved by pip.
#
# This replaces an earlier `pip freeze` dump that also carried the whole
# JupyterLab stack. The repository contains no notebooks - every analysis is a
# plain module in src/ - and those extra packages additionally broke `make setup`
# on Windows by exceeding the default path-length limit.

# Analysis and figures (all `make` targets)
numpy==2.4.2
pandas==3.0.1
scikit-learn==1.8.0
scipy==1.17.1
matplotlib==3.10.8

# Interpretability only (`make interpret`)
shap==0.52.0

# Dataset download only (`make download`); the CSV is committed under data/raw/,
# so this is optional for reproducing the study.
kagglehub==1.0.0
